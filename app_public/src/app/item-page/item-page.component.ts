import { Component } from '@angular/core';

@Component({
  selector: 'app-item-page',
  templateUrl: './item-page.component.html',
  styleUrl: './item-page.component.css'
})
export class ItemPageComponent {
  //Item needs:
  /* 
  current user/see if signed in
  if user is not signed in throw a error for the add to cart button
  display items price, img, name and descption.  
  
  
  
  */
}
